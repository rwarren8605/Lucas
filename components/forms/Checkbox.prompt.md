**Checkbox** — rounded-square checkbox; black fill + white check when on.

```jsx
<Checkbox checked label="I agree to the terms" onChange={setChecked} />
```

`checked`, `label`, `onChange(next)`, `disabled`, `size`.
