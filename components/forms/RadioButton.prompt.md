**RadioButton** — single-select option; black ring + filled dot when on, red ring on `error`.

```jsx
<RadioButton checked label="Length of credit history (15%)" onChange={pick} />
```

`checked`, `label`, `onChange`, `disabled`, `error`, `size`.
