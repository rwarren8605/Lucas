**MobileFooter** — sticky bottom action bar with a primary button and an optional secondary beneath it; safe-area aware.

```jsx
<MobileFooter
  primary={<Button type="primary" fullWidth>Continue</Button>}
  secondary={<Button type="secondary" fullWidth>Maybe Later</Button>}
/>
```

`primary`, `secondary`, plus free `children` above the buttons.
