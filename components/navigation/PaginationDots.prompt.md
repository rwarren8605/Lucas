**PaginationDots** — onboarding / carousel position. Active dot stretches into a yellow pill.

```jsx
<PaginationDots count={4} active={1} onDotClick={setStep} />
```

`count`, `active`, `onDotClick`.
