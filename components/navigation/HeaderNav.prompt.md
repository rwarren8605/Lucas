**HeaderNav** — the screen's top bar: back chevron, centered title, optional trailing action. `tone="brand"` for the yellow celebration screens.

```jsx
<HeaderNav title="Module 5 of 5" onBack={goBack} trailing={<IconButton icon="more_horiz" type="ghost" />} />
```

`title`, `onBack`, `trailing`, `tone` (default/brand).
