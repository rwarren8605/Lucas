**Tabs** — underline tab bar; active tab is bold black with a 2.5px underline.

```jsx
<Tabs tabs={["Overview","Modules","Resources"]} value={tab} onChange={setTab} />
```

`tabs` (strings or `{label,value}`), `value`, `onChange`.
